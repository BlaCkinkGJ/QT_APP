#include "buffer.h"

#include <QDebug>

buffer::buffer(){
    int nothing = 0;
    // ready to save state
}



void buffer::writeToFile(QDataStream& stream){
    int nothing = 0;
}

void buffer::readFromFile(QDataStream& stream){
    int realNothing = 1;
    // something to write
}
